# Memoery

- Project name: HustleSync
- Firebase project ID: `hustlesync-3665b`
- Firebase Hosting is configured for `dist/`
- The app uses Anonymous Auth and Firestore
- Local environment variables live in `.env`
- Main app source lives in `src/App.jsx`
- The latest working build passed with Vite 8, React 19, Firebase 12, and Tailwind 4
- `firebase` CLI is installed locally through `firebase-tools`